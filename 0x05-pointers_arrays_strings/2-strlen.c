#include "main.h"

/**
 * _strlen - returns length of a string
 * @s: Arguments of function
 * Return: Returns length of string
 */

int _strlen(char *s)
{
	int len;
	len = strlen(*s);
	return (len);
}
