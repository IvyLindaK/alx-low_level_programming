#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: First argument
 * @b: Second argument
 * Return: result of sum
 */

int add(int a, int b)
{
	int sum = 0;

	sum = a + b;

	return (sum);
}
